name = "Python Lint"
version = "2.1.0"
description = "Run ruff/pyright on Python files (supports project venv)"

[permissions]
execute = ["ruff", "pyright"]

[[menu_items]]
label = "Run Lint"
action = "lint"
shortcut = "Ctrl+Shift+P"
